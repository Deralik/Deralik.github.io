# Local-only privacy strings for the pre-commit hook (one per line):
# put the real phone number etc. here so the hook can catch them without
# the strings ever being committed themselves.
.privacy.local
.DS_Store
node_modules/
# Local-only truth copies + design-context binaries — never committed.
design/local/
