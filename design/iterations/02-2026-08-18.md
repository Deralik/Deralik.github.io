# Round 02 — 2026-08-18 — handoff import + workflow protocols

Covers the design-project rounds since iterations/01 (run inside the
claude.ai "Portfolio Design System" project, roughly 2026-08-02 →
2026-08-18) and the day their handoff came back to the repo.

## What the design rounds produced (imported via handoff zip)

- The **depth machine**: one page, three depths (D0 dashboard / D1 panel
  / D2 document), About + 7 fields, weighted partition in two bands,
  separate mobile machine. High-fidelity working skeleton in HTML/CSS/
  vanilla JS (~4.5k lines, dependency-free, figures are live canvas
  algorithms).
- **About and cINR sections finished; GRTCache ported but rough** (owner:
  "we will be refining it a lot"). Three empty band-2 slots.
- **Theming settled**: 7 exploration themes → production ships light
  `h-transit` + dark `k-matrix` with a toggle.
- A living rulings log (two registers) — now continued as
  design/RULINGS.md, canonical in this repo.
- Port spec: design/handoff/ (README = production tasks, skeleton-notes,
  DATA-HANDOFF for the GRT real-data swap).

## Import-gate findings (privacy)

- The zip's `DZResume.pdf` contains the owner's phone → NOT the
  email-only variant; excluded permanently; check.mjs now extracts PDF
  text so this class of miss can't recur.
- `about-record.json` `identity.phone` → strip at port (renderer never
  reads it; verified by grep).
- Side finding for the private repo: the résumé's "AS, GPA 3.95" line
  disagrees with the transcript per the design round's own source audit
  (cum 3.879; span 2017–2021; econ credential differs) — likely a paste
  error; owner to fix at the source.

## Decisions (owner, 2026-08-18) — recorded in RULINGS, law there

Launch posture (honest partial launch, GRT on real data, titled
under-construction slots) · band-2 order MelioraOS → Okibi → SPLAT ·
GRT fact pack rewritten now from the manuscript record · release
protocol (check gate → changelog → owner pushes) + CI · verify tooling
(probe matrix incl. 390px + both themes) · freshness sweeps
(SOURCES.md) · nothing-is-gospel challenge protocol · protocol carried
by machinery, not model memory.

## Repo work this round (S0 + S1)

Docs: BRIEF fact packs updated (GRT manuscript record; Meliora beta.11 +
private-beta; structure section replaced the page inventory; demos
rewritten honest) · CLAUDE.md drift fixed + working agreements ·
ROADMAP rewritten to reality · SYSTEM.md filled from the settled design
· PROMPTS rewritten (5 cold-start prompts) · new: CONTRACT.md,
REVIEW.md, SOURCES.md, RULINGS.md, handoff/, working/.
Machinery: scripts/check.mjs, scripts/probe.mjs, .claude/ (reviewer
agent, review + verify skills, hooks, denies), CI workflow, pre-commit
extension, CHANGELOG.md.

## Open questions → next

- S2 retrofit audit (fresh reviewers) before the port: what do the demos
  *actually* compute; contract audit of About/cINR/GRT; skim test.
- GRT real-data work: nebula dataset selection + offline conversion;
  black-hole feasibility check with options to owner.
- MelioraOS vs Meliora naming (carried from RULINGS model notes).
- Probe smoke test surfaced two seeds, both RESOLVED same day:
  (a) title → owner ruled "GPU & Machine Learning engineer" (mockup
  wins; BRIEF updated; about-record harmonized at port); (b) "2× avg
  path-trace" → owner-confirmed AND paper-verified ("a roughly 2x gain
  for PT"; record-table PT ratios average ≈2.0×); BRIEF carries it.
  Bonus: "over 90 FPS" provenance pinned — DNS case study, 91.8 avg FPS
  ray-marching with the cache.
- Primary sources pinned in SOURCES.md: both papers, the GRT
  manuscript, the HPG '26 reviews (summary objection: no equal-time
  comparison + validation clarity — the page's paraphrase target), and
  gsrc's post-manuscript technical record (2026-08-06).
- Cleanliness ruling applied: gitignored design/local/ created (sources
  + captures at stable paths); the 16 Meliora/Okibi captures left the
  public tree (history keeps them — vetted fixture shots, cleanliness
  not privacy); .gitkeeps dropped from non-empty dirs.
